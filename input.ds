fn main () {
    let s1: set = [1, 2, 4];
    let s2: set = [2, 3, 4];
    let a: int = 1;
    let b: int = 3;
    if (a > b) {
        println(s1 + s2);
    }
    else {
        println(s1 * s2);
    }
    for i in 1..5 {
        print(i);
    }
}